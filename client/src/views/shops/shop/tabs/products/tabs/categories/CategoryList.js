import React from 'react'
import DataTable from 'react-data-table-component'
import { Card } from 'reactstrap'

export default function CategoryList({store,dispatch}) {
    // const columns = [
    //     {
    //         name: 'Category',
    //         sortable: true,
    //         width: '100px',
    //         //sortField: 'name',
      
    //         selector: (row) => row.imgUrl,
    //         cell: (row) => <img src={row.imgUrl} className="w-100" />
    //       },
    // ]
  return (
    <>
    {/* <Card>
        <div className="react-dataTable" style={{ height: '73.5vh' }}>
          {data && (
            <DataTable className="react-dataTable" columns={columns} data={data} pagination />
          )}
        </div>
      </Card> */}
    </>
  )
}
